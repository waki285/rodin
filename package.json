{
  "name": "rodin",
  "private": true,
  "scripts": {
    "build": "echo Use cargo to build this project! && exit 1",
    "start": "echo Use cargo to run this project! && exit 1"
  },
  "devDependencies": {
    "esbuild": "^0.27.0"
  }
}
